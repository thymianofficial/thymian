name: CI
on:
  push:
    branches:
      - main
  pull_request: {}
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          filter: tree:0
      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: install
        run: npm i --ignore-scripts
      - uses: nrwl/nx-set-shas@v4
      - name: run linter
        run: npx nx affected -t lint

  main:
    strategy:
      matrix:
        version: [20, 22, 24]
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          filter: tree:0
      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.version }}
          cache: npm
      - name: install
        run: npm ci
      - uses: nrwl/nx-set-shas@v4
      - name: Build
        run: npx nx run-many -t build --exclude @thymian/source
      - name: Test
        run: npx nx affected -t test
