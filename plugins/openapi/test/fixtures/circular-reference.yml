openapi: 3.0.0
info:
  title: Hello World
  version: 1.0.0
paths:
  /test:
    get:
      responses:
        '200':
          description: foobar
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Test'
components:
  schemas:
    Test:
      type: object
      properties:
        test:
          $ref: '#/components/schemas/Test'
